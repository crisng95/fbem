"""FBEM — Facebook Extension Crawler + MCP.

A standalone tool that crawls/snapshots Facebook's *native composer* web API
(the requests a logged-in human makes on facebook.com) and replays them to
publish Reels / Photos — via a Chrome extension, exposed to any agent as an MCP
server. See README.md.
"""

__version__ = "0.1.0"
